#ifndef RANDOM_H
#define RANDOM_H

/*  Generates a random number between zero and max,
    inclusively, based on a uniform distribution
*/
int randomInt(int max);

/*  Generates a random number between min and max,
    inclusively, based on a uniform distribution
*/
int randomInt(int min, int max);

#endif 
